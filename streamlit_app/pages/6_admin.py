import streamlit as st

from theme import apply_theme

apply_theme()

st.title("Admin")
st.caption("Edycja progów i hiperparametrów pipeline'u — do zbudowania później (logowanie, zapis do pipeline_config).")
